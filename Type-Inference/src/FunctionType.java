public class FunctionType extends Type {
    
    public FunctionType() {
        
    }

    public boolean equals(Object o) {
        if (o instanceof FunctionType) {
            return true;
        }
        return false;
    }
}
