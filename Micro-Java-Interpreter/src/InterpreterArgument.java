public class InterpreterArgument {
    Environment env;
    Store store;
    
    public InterpreterArgument(Environment env, Store store) {
        this.env = env;
        this.store = store;
    }
}
